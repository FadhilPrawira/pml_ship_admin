import 'package:flutter/material.dart';
import 'package:pml_ship_admin/theme.dart';

class OrderDataPage extends StatelessWidget {
  const OrderDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('OrderDataPage'),
      ),
    );
  }
}
